# 로컬 개발용 (docker compose의 postgres 컨테이너)
# 운영 DB 접속 정보는 k8s/secret.yaml에서 관리
DB_HOST=localhost
DB_PORT=5432
DB_NAME=budget_db
DB_USER=budget_book
DB_PASSWORD=local

# 카테고리 자동 분류 (없으면 분류 실패 시 "기타" 처리)
ANTHROPIC_API_KEY=your_api_key_here
