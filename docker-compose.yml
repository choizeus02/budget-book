services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    env_file:
      - ./api/.env
    develop:
      watch:
        - action: sync+restart
          path: ./api
          target: /app

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api

